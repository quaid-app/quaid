# Leela — Link Contract Review & Type Corrections

**Date:** 2026-04-14T03:50:40Z
**Status:** Completed
**Branch:** phase1/p1-core-storage-cli

## Outcome

- ✅ Link contract ambiguity resolved: slugs at app layer, integer FKs in DB (standard view/data separation)
- ✅ Three data-loss bugs corrected in `src/core/types.rs`:
  - `Link.context: String` (was missing)
  - `Link.id: Option<i64>` (was i64, sentinel value problem)
  - `Page.truth_updated_at` and `Page.timeline_updated_at` (were missing)
- ✅ Task spec clarification written to `tasks.md` (slug contract documented at task 2.2)
- ✅ Bender's validation checklist updated with correct expectations

## Key Decision

**Slugs are the correct Phase 1 contract for `Link`.** Integer page IDs remain internal DB plumbing.
- Callers (CLI, MCP) always work with slugs
- DB layer resolves slugs → IDs on insert, IDs → slugs on read
- Standard data model / view model separation; no architectural change needed

## Routing

- **Fry:** `Link` and `Page` structs now authoritative; use corrected fields in all db.rs read/write paths (T03+)
- **Bender:** validation checklist updated; Link section now expects slugs, `context`, `id: Option<i64>`
- **Professor:** no spec review needed; schema correct, task was under-specified

## Unblocked

Phase 1 proceeds. No ambiguity remains. Three data-loss bugs corrected at source before spread.
