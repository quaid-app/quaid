# Fry — T02 Database Layer (src/core/db.rs)

**Date:** 2026-04-14T03:50:40Z
**Status:** Completed
**Branch:** phase1/p1-core-storage-cli

## Outcome

- ✅ `src/core/db.rs` implemented: `open()`, `init_db()`, pragma setup, sqlite-vec auto-extension
- ✅ Tests: db layer passes all unit tests
- ✅ Validation: `cargo check`, `cargo clippy`, `cargo test` — clean
- ✅ Decision record filed: `fry-p1-db-slice.md` (schema DDL approach, sqlite-vec registration, error types)

## Key Decisions

1. **sqlite-vec** loaded via `sqlite3_auto_extension` (process-global, acceptable for single-binary CLI)
2. **Error types** use `thiserror` (core/ layer boundary enforcement)
3. **Schema DDL** applied as-is from `schema.sql` (single source of truth, PRAGMAs included)
4. **Link schema** uses integer FKs internally (`from_page_id`, `to_page_id`); struct maps to/from slugs at app layer

## Handoff

- **Fry next:** T03 markdown parsing (`src/core/markdown.rs`)
- **Leela:** Contract review (Link struct + Page schema alignment) — **filed; see leela-link-contract-review.md**
- **Bender:** T02 validation plan ready; awaiting T02-T06 code

## Notes

Leela's review identified three data-loss bugs in the original task spec and corrected them:
- `Link.context` field (was missing, added)
- `Link.id` type corrected to `Option<i64>` (sentinel value issue)
- `Page` timestamp fields corrected (`truth_updated_at`, `timeline_updated_at` were missing)

All three fixes applied to `src/core/types.rs` before Phase 1 spread further. No schema changes needed.
