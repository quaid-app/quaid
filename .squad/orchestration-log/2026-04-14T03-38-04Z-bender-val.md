# Bender — Phase 1 Foundation Validation Plan

**Date:** 2026-04-14T03:38:04Z  
**Agent:** Bender  
**Status:** Anticipatory (validation triggers on tasks 2.1–2.6 completion)

## What

Prepared comprehensive validation plan for `src/core/types.rs` (tasks 2.1–2.6):

### Validation Scope

- **Schema–Struct Alignment:** All `pages` and `links` schema columns matched to Rust fields
- **Error Enum Hygiene:** `OccError::Conflict` variant with `current_version` field; `thiserror` derives
- **SearchMergeStrategy Exhaustiveness:** SetUnion + Rrf variants, parseable from config
- **Derive Trait Completeness:** Debug, Clone, Serialize, Deserialize as needed
- **Compilation Gate:** `cargo check` exit 0

### Flagged Ambiguity

**Link Schema Mismatch:**
- Task 2.2 specifies: `from_slug`, `to_slug`
- Schema specifies: `from_page_id`, `to_page_id` (integer FK)
- Decision: DB layer resolves slugs to IDs; Link struct holds slugs for ergonomics

### Edge Cases to Verify Later

1. Empty slug validation (defer to DB layer phase)
2. Version = 0 handling (enforce >= 1 at DB insert?)
3. Frontmatter type fidelity (HashMap vs String)
4. Timestamp format validation (String vs chrono type)
5. Link temporal validity constraints
6. `type` keyword collision handling
7. SearchResult score type (f64 for BM25 + cosine)

## Execution Plan

Once tasks 2.1–2.6 land:

1. Run `cargo check` — hard gate
2. Run `cargo clippy -- -D warnings` — CI parity
3. Manually diff Page fields vs pages schema (14-column checklist)
4. Manually diff Link fields vs links schema (8-column checklist + context field)
5. Verify error enum contracts
6. Verify SearchMergeStrategy variants
7. File pass/fail verdict

**Estimated time:** 15 minutes once code lands

## Artifacts

- Plan: `.squad/decisions/inbox/bender-p1-foundation-validation.md`

## Next Phase

Validation executes in parallel with Fry's database layer slice (tasks 3.1–3.5).
