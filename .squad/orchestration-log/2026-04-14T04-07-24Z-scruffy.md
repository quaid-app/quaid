# Scruffy Orchestration — 2026-04-14T04:07:24Z

**Phase:** Phase 1 command surface expansion  
**Status:** BLOCKED — implementation not ready; coverage plan locked  
**Completed in this window:** T06 unit test expectations specification

## Execution Summary

### T06 put Command Unit Test Specification (LOCKED)
- Documented 3 core test cases: create, update with matching version, conflict with stale version
- Locked coverage targets before implementation lands
- Specified required seam for testability: pure helper + thin CLI wrapper

## Coverage Targets (NOT YET TESTED)

### Test 1: Create path
- Parse raw markdown via `parse_frontmatter` + `split_content`
- Derive title, page_type, summary, wing, room, compiled_truth, timeline
- Store version = 1
- Fixture shape specified: canonical markdown with timeline containing later `---`

### Test 2: Update path (OCC success)
- Insert initial page at version = 1
- Call put with `expected_version = Some(1)` and changed markdown
- Verify update succeeds, version becomes 2, slug stable, content fully replaced

### Test 3: Conflict path (OCC failure)
- Insert page at version = 2
- Call put with `expected_version = Some(1)`
- Verify: conflict returned, row unchanged, version remains 2
- Classification: Bug-prone branch; guard hard

## Implementation Requirements
- No monolithic stdin/stdout function; extract pure helper
- Helper signature: `&Connection, slug, raw markdown, expected_version` → result version or OCC error
- CLI `run()` wraps helper as thin formatter

## Test Naming Convention
- `put_creates_page_from_stdin_markdown_with_version_one`
- `put_updates_existing_page_when_expected_version_matches`
- `put_returns_conflict_and_preserves_row_when_expected_version_is_stale`
- `put_derives_summary_wing_and_room_from_markdown_and_slug`

## Assertion Guards
1. Frontmatter: compare deserialized maps, not raw JSON
2. Markdown split: assert exact truth/timeline values, boundary newlines
3. OCC semantics: stale version must fail without mutation
4. Phase 1 room: stored as empty string even with headings

**Status:** Ready for implementation. Awaiting Fry's T06 code land.
