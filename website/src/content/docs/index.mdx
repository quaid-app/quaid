---
title: GigaBrain
description: Your brain, persisted. Zero cloud, zero keys. SQLite + FTS5 + vector embeddings in one static binary.
template: splash
hero:
  title: "Your brain, persisted. <br><span class='text-primary'>Zero cloud, zero keys.</span>"
  tagline: A high-performance local knowledge engine that turns your files into a searchable, intelligent second brain without ever leaving your machine.
  actions:
    - text: Get Started
      link: /guides/quick-start/
      icon: right-arrow
      variant: primary
    - text: View Source
      link: https://github.com/macro88/gigabrain
      icon: github
      variant: secondary
---

import { Code, CardGrid, Card } from "@astrojs/starlight/components";

## Install

<Code code={`curl -fsSL https://raw.githubusercontent.com/macro88/gigabrain/main/scripts/install.sh | sh`} lang="bash" title="Install GigaBrain (macOS / Linux)" />

Then import your notes and connect to Claude Code:

<Code code={`gbrain import ~/docs --db ~/brain.db\ngbrain serve --db ~/brain.db`} lang="bash" />

**Three commands. No API keys. No cloud.** [Full install options →](/guides/install/)

---

## Architecture for Privacy

<CardGrid>
  <Card title="SQLite Everything" icon="seti:db">
    No complex setup. Your entire knowledge graph is stored in a highly
    optimized SQLite database on your local disk. Lightweight, portable, and
    permanent.
  </Card>
  <Card title="Local Embeddings" icon="star">
    Run state-of-the-art vector embeddings locally. Your private data never
    touches an API. Privacy-first AI that works completely offline.
  </Card>
  <Card title="Hybrid Search" icon="magnifier">
    Combine the precision of keyword search with the context of semantic search.
    Find exactly what you need across millions of documents in milliseconds.
  </Card>
  <Card title="Live Vault Sync" icon="rocket">
    Attach any Obsidian vault as a collection. GigaBrain watches for edits
    with a 1.5 s debounce and reconcile-backed flushes so the brain stays
    current as you write. <em>(Unix/macOS/Linux only — v0.9.6)</em>
  </Card>
</CardGrid>

---

## Get running in seconds

Three commands. No API keys. No cloud.

```bash
# Initialize a brain
gbrain init ~/brain.db

# Import your notes (idempotent — safe to re-run)
gbrain import ~/notes --db ~/brain.db

# Expose 17 tools to any MCP client over stdio (Unix/macOS/Linux; v0.9.6)
GBRAIN_DB=~/brain.db gbrain serve
```
