# Fry — Phase 1 Core Storage CLI Complete (Tasks 21–34)

**Date:** 2026-04-15T02:16:00Z  
**Agent:** Fry (Main Engineer)  
**Status:** Complete  
**Branch:** phase1/p1-core-storage-cli  
**PR:** #12

## What

Implemented all remaining Phase 1 tasks (T21–T34) with foundational storage layer, full MCP server surface, import/export CLI, and round-trip test suite.

### Completed Tasks

- **T21:** `src/core/migrate.rs` — import/export directory traversal, SHA-256 idempotency, fixture validation
- **T22–T28:** CLI commands — `ingest`, `timeline`, `import`, `export`, `config`, `version` with `--json` flags
- **T29:** MCP server stdio — all tools registered, error code mapping, threading model
- **T30:** Round-trip tests — byte-exact fixture validation, raw export roundtrip
- **T31–T32:** `--json` flags — all commands emit JSON on demand
- **T33–T34:** Fixture set and docstring audit

## Quality Gates

- ✓ 142 tests passing (baseline 64 + T20/T21 slices 78 new)
- ✓ `cargo clippy --all-targets -- -D warnings` — 0 warnings
- ✓ `cargo fmt --check` — clean
- ✓ Roundtrip test suite validated against 12 real fixture files

## Key Decisions Logged

1. **import_hashes table** — Separate from `ingest_log`. File-level SHA-256 dedup for CLI ingestion; `ingest_log` tracks MCP/API events.
2. **MCP threading** — `Arc<Mutex<Connection>>` for rmcp trait compliance; no contention in single-threaded stdio.
3. **Error codes** — Custom JSON-RPC codes: `-32009` (OCC), `-32001` (not found), `-32002` (parse), `-32003` (DB).
4. **Fixture format** — LF line endings, sorted frontmatter, unquoted values. Matches `render_page()` canonical output.
5. **Timeline command** — Raw markdown splits on `---`; no structured table query.
6. **Skill files** — Updated `skills/ingest/SKILL.md` and `skills/query/SKILL.md` to reflect Phase 1 surface.

## Test Coverage

- **Total:** 142 passing
- **New in phase 1 complete:** 78 tests across migrate, CLI, MCP, roundtrip slices
- **Linting:** 0 clippy warnings, 0 fmt issues

## Artifacts

- Code: `src/core/migrate.rs`, `src/commands/{ingest,timeline,import,export,config,version}.rs`, `src/mcp/server.rs`
- Tests: `tests/roundtrip_raw.rs`, `tests/roundtrip_semantic.rs`, command unit tests
- Fixtures: 12 markdown files in `tests/fixtures/`
- Decision: `.squad/decisions/inbox/fry-phase1-complete.md`

## Blockers Resolved

- Round-trip tests: `src/lib.rs` exports finalized
- Phase 1 gate: All 34 tasks complete and testing green

## Next: Phase 2

Phase 1 gate is passed. Ready for Professor and Nibbler approval. Phase 2 intelligence layer (knowledge_gaps, assertions, graph, progressive retrieval) waiting on Phase 1 ship signal.

**Branch:** phase1/p1-core-storage-cli  
**PR:** #12 ready for merge
