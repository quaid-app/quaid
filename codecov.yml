coverage:
  status:
    patch:
      default:
        target: 80%
        threshold: 5%
    project:
      default:
        target: auto
        threshold: 1%

ignore:
  - "tests/**"
  - "src/commands/mod.rs"
