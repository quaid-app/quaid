# Rust build artifacts
/target/

# Local SQLite working data
/brain.db
/brain.db-journal
/brain.db-shm
/brain.db-wal

# Local environment overrides
.env
.env.*

# Cross-platform OS cruft
.DS_Store
Thumbs.db

# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# Benchmark datasets (downloaded by prep_datasets.sh — never committed)
benchmarks/datasets/

# npm postinstall downloaded binary
packages/gbrain-npm/bin/gbrain.bin
packages/gbrain-npm/bin/gbrain.download
